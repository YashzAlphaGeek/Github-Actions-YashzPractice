name: "Workflow Commands"

on:
  push:
    branches:
      - main

jobs:
  context-job:
    runs-on: ubuntu-latest
    steps:
      - name: "Step 1"
        run: echo "Hello! {github.action}"
        env:
            MY_ACTION: ${{github.action}}